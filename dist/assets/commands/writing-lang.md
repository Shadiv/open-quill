---
description: Set per-project default output language (Open Quill)
agent: writer
---
Set the per-project default output language to `$ARGUMENTS`.

Call the `set_project_language` tool with:
- language: `$ARGUMENTS`

Then confirm to the user that the project default output language has been set.
